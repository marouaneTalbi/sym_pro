{% extends 'base.html.twig' %}

{% block title %}LIST{% endblock %}

{% block body %}

<div class="text-end p-2 "><a href="{{path('app_add')}}"><button class=" btn btn-warning"><i class="fas fa-plus"></i></a></button></div>
<div class="col-8 offset-6 p-2 form-group ">

{{form_start(form_search)}}
{{form_widget(form_search)}}
<button class="btn btn-secondary " type="submit"><i class="fas fa-search"></i></button>
{{form_end(form_search)}}

</div>


<table class="table table-striped">

    <thead>
        <tr >
            <th>Id</th>
            <th>Marque</th>
            <th>Modele</th>
            <th>Pays</th>
            <th>Prix</th>
            <th>Categorie</th>
            <th>Image</th>
            <th>Description</th>
            <th colspan="2">Action</th>
        </tr>
    </thead>
    <tbody>
    {% for car in tabcars %}

<tr>
    <td>{{car.id}}</td>
    <td>{{car.Marque}}</td>
    <td>{{car.Modele}}</td>
    <td>{{car.pays}}</td>
    <td>{{car.prix}} €</td>
    <td>{{car.category.nom}}</td>
    <td><img src="{{ asset('images/' ~ car.image)}}" alt="" width="100"/></td>

    <td>{{car.description}}</td>
 <th><a class="btn btn-warning" href="{{path('app_edit', {id: car.id}) }}"><i class="fas fa-edit"></i> Edit</a></th>
        <th><a class="btn btn-danger" onclick="return confirm('Etes vous sur de vouloir suprimer pas')" href="{{path('app_delete', {id: car.id}) }}"><i class="fas fa-trash"></i> Supprimer</a></th> 
     </tr>
     {% endfor %}

    </tbody>

</table>

{% endblock %}